#
# Sets up components needed for using the code
#

import subprocess

# Make a directory to store executables (if it doesn't already exists)
subprocess.call(['mkdir executables'], shell=True)

# Optionally add to the environment (later)
